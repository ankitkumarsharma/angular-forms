export interface ReactiveFormType {
  firstName: string,
  lastName: string,
  email: string,
  address: string
}
export interface ReusableFormType {
  firstName: string,
  lastName: string,
  email: string,
  address: string,
  userListControl: string
}
